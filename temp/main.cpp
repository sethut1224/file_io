#include "file_io.hpp"
#include <iostream>
#include <chrono>


int main()
{
    // FileIO fio;
    // std::filesystem::path p("/home/sethut/file_io/test.txt");
    // std::filesystem::path p2("/home/sethut/file_io/test2.txt");
    // std::filesystem::path p3("/home/sethut/file_io/a/b/c/d/");
    // std::string buf, buf2;

    // fio.open(p);
    // fio.read(buf);
    // fio.write(buf);
    // fio.read(buf2);
    // fio.copy(p, p2, std::filesystem::copy_options::skip_existing);
    // fio.rmdir(p2);
    // fio.mkdir(p3);

    // std::cout<<"size : "<<fio.size(p)<<std::endl;
    // std::cout<<"exist : "<<std::boolalpha<<fio.exist(p)<<std::endl;
    // std::cout<<"current path : "<<fio.current_path(p)<<std::endl;
    // std::cout<<"absoulte path : "<<fio.absolute_path(p)<<std::endl;
    // std::cout<<"canonical path : "<<fio.canonical_path(p)<<std::endl;
}